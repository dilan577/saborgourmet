doctype html
html(lang="es")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title= titulo
    link(rel="stylesheet" href="/css/style.css")

  body.bg-gray-100.min-h-screen.flex.items-center.justify-center.py-12
    .max-w-md.w-full.space-y-8.p-8
      .card
        // 🧾 ENCABEZADO
        div
          h2.text-center.text-3xl.font-extrabold.text-gray-900 Crear Cuenta
          p.mt-2.text-center.text-sm.text-gray-600 Regístrate en SaborGourmet
        
        // 🔴 MENSAJE DE ERROR
        if error
          .mt-4.bg-red-100.border.border-red-400.text-red-700.px-4.py-3.rounded.text-center
            p= error
        
        // 📝 FORMULARIO REGISTRO
        form.mt-8.space-y-6(
          action="/auth/registro"
          method="POST"
          autocomplete="off"
        )
          .space-y-4
            div
              label.label(for="nombre") Nombre
              input#nombre.input-field(
                type="text"
                name="nombre"
                required
                placeholder="Tu nombre"
                autocomplete="off"
              )
            
            div
              label.label(for="apellido") Apellido
              input#apellido.input-field(
                type="text"
                name="apellido"
                required
                placeholder="Tu apellido"
                autocomplete="off"
              )
            
            div
              label.label(for="email") Correo Electrónico
              input#email.input-field(
                type="email"
                name="email"
                required
                placeholder="correo@ejemplo.com"
                autocomplete="off"
              )
            
            div
              label.label(for="telefono") Teléfono
              input#telefono.input-field(
                type="tel"
                name="telefono"
                placeholder="Opcional"
                autocomplete="off"
              )
            
            div
              label.label(for="password") Contraseña
              input#password.input-field(
                type="password"
                name="password"
                required
                placeholder="••••••••"
                autocomplete="new-password"
              )
            
            div
              label.label(for="confirmarPassword") Confirmar Contraseña
              input#confirmarPassword.input-field(
                type="password"
                name="confirmarPassword"
                required
                placeholder="••••••••"
                autocomplete="new-password"
              )
          
          // 🔘 BOTÓN
          div
            button.btn-primary.w-full(type="submit") Registrarse
          
          // 🔗 LINK LOGIN
          .text-center
            a.text-sm.text-primary-600.hover_text-primary-500(href="/auth/login")
              | ¿Ya tienes cuenta? Inicia sesión
