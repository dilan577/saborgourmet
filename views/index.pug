doctype html
html(lang="es")
  head
    title Bienvenidos a SaborGourmet
    link(rel="stylesheet", href="/css/style.css")
  body
    h1 ¡Bienvenidos a SaborGourmet!
    p Para realizar una reserva, por favor:
    a(href="/auth/login") Iniciar sesión
    br
    a(href="/auth/registro") Registrarse
    hr
    h2 Reserva sin cuenta
    form(action="/reservas/crear" method="POST")
      label(for="fecha") Fecha:
      input(type="date" name="fecha" required)
      label(for="hora") Hora:
      input(type="time" name="hora" required)
      label(for="numeroPersonas") Número de personas:
      input(type="number" name="numeroPersonas" min="1" required)
      button(type="submit") Crear reserva

